const AddToFavoritesIconDescription = ({ text, className, style }) => {
  return (
    <div
      className={className}
      style={style}
    >
      {text}
    </div>
  );
};
export default AddToFavoritesIconDescription;
